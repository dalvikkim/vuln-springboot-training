version: '3.8' 

services:
  vuln-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:h2:mem:testdb
      SPRING_H2_CONSOLE_ENABLED: "true"
    # 교육용: 컨테이너 내부에서만 사용 시 restart 제거 가능
    restart: unless-stopped
